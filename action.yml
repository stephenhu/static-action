# action.yml
name: "static action"
description: "convert templates into html"
inputs:
  path:
    description: "source file path"
    required: true
    default: "."
outputs:
  files:
    description: "generated static filenames"
runs:
  using: "docker"
  image: "docker://ghcr.io/stephenhu/static:latest"
